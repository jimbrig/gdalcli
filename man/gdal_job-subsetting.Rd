% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-gdal_job.R
\name{gdal_job-subsetting}
\alias{gdal_job-subsetting}
\alias{[.gdal_job}
\alias{length.gdal_job}
\alias{[<-.gdal_job}
\alias{$<-.gdal_job}
\alias{[[<-.gdal_job}
\alias{c.gdal_job}
\alias{names.gdal_job}
\title{Subsetting and Access Methods for GDAL Jobs}
\usage{
\method{[}{gdal_job}(x, i)

\method{length}{gdal_job}(x)

\method{names}{gdal_job}(x)

\method{$}{gdal_job}(x, name) <- value

\method{[[}{gdal_job}(x, i) <- value

\method{[}{gdal_job}(x, i) <- value

\method{c}{gdal_job}(..., recursive = FALSE)
}
\arguments{
\item{x}{A \code{gdal_job} object.}

\item{i}{Index, property name, or value.}

\item{name}{Property name for \code{$} and \verb{$<-}.}

\item{value}{New value for assignment operators.}

\item{...}{Multiple \code{gdal_job} objects for \code{c()}.}

\item{recursive}{Ignored (for S3 consistency).}
}
\description{
Provides subsetting operators for \code{gdal_job} objects.
\itemize{
\item \code{[} extracts specific job(s) from a pipeline, returning a \code{gdal_job}
\item \code{length()} returns the number of pipeline jobs (or 1 if standalone)
\item \code{c()} combines multiple jobs into a pipeline
\item \code{$} and \code{[[} use standard list semantics for property access
}
}
\examples{
\dontrun{
pipeline <- gdal_raster_reproject(input = "input.tif", dst_crs =
"EPSG:32632") |>
  gdal_raster_scale(src_min = 0, src_max = 100) |>
  gdal_raster_convert(output = "output.tif")

length(pipeline)           # Returns 3
pipeline[1]                # First job
pipeline[1:2]              # First two jobs
pipeline$command_path      # Access property
pipeline$config_options <- c("OPT=VALUE")  # Set property

job1 <- gdal_raster_reproject(input = "in.tif", dst_crs = "EPSG:32632")
job2 <- gdal_raster_scale(src_min = 0, src_max = 100)
job3 <- gdal_raster_convert(output = "out.tif")
combined <- c(job1, job2, job3)  # Combine into pipeline
}

}
