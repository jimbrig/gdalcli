% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-explicit-args.R
\name{gdal_job_run_with_audit}
\alias{gdal_job_run_with_audit}
\title{Run GDAL Job with Audit Trail}
\usage{
gdal_job_run_with_audit(
  job,
  ...,
  audit_log = getOption("gdalcli.audit_logging", FALSE)
)
}
\arguments{
\item{job}{A \code{gdal_job} object to execute}

\item{...}{Additional arguments passed to \code{\link[=gdal_job_run]{gdal_job_run()}}}

\item{audit_log}{Logical. If TRUE, creates audit trail. Default TRUE if
getOption("gdalcli.audit_logging") is TRUE.}
}
\value{
Result from \code{\link[=gdal_job_run]{gdal_job_run()}} with audit information attached
as attribute "audit_trail"
}
\description{
Enhanced version of \code{\link[=gdal_job_run]{gdal_job_run()}} that captures explicit arguments
in an audit log for reproducibility and debugging.
}
\details{
The audit trail captures:
\itemize{
\item Timestamp of execution
\item Command and explicit arguments
\item GDAL version used
\item R version used
\item Execution status and any errors
}

Audit trails are stored as attributes on the result object and can be
accessed with \code{attr(result, "audit_trail")}.

Audit logging is controlled by \code{options(gdalcli.audit_logging = TRUE/FALSE)}.
When FALSE (default), no audit trail is created even if this function is
called.
}
\examples{
\dontrun{
  job <- new_gdal_job(
    command_path = c("raster", "info"),
    arguments = list(input = "test.tif")
  )

  # Enable audit logging
  options(gdalcli.audit_logging = TRUE)
  result <- gdal_job_run_with_audit(job)

  # Inspect audit trail
  audit <- attr(result, "audit_trail")
  cat("Explicit args used:\n")
  print(audit$explicit_args)
}

}
