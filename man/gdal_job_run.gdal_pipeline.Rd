% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-gdal_pipeline.R
\name{gdal_job_run.gdal_pipeline}
\alias{gdal_job_run.gdal_pipeline}
\title{Check if Path is Virtual File System
Execute a GDAL Pipeline}
\usage{
\method{gdal_job_run}{gdal_pipeline}(
  x,
  execution_mode = c("sequential", "native"),
  stream_in = NULL,
  stream_out_format = NULL,
  env = NULL,
  checkpoint = NULL,
  checkpoint_dir = NULL,
  resume = FALSE,
  ...,
  verbose = FALSE
)
}
\arguments{
\item{x}{A \code{gdal_pipeline} object.}

\item{execution_mode}{Character string: \code{"sequential"} (default) to run jobs as
separate GDAL commands, or \code{"native"} to run as a single native GDAL pipeline
command (via \verb{gdal raster/vector pipeline}). Native mode is more efficient
for
large datasets as it avoids intermediate disk I/O.}

\item{stream_in}{An R object to be streamed to \verb{/vsistdin/} for the first pipeline
step.
Only used in native execution mode. Default \code{NULL}.}

\item{stream_out_format}{Character string: output format for the last pipeline step.
Options: \code{NULL} (no streaming), \code{"text"}, or \code{"raw"}. Only used in native
execution mode.}

\item{env}{Environment variables to pass to the GDAL process. Only used in native
execution mode.}

\item{checkpoint}{Logical. Override global checkpoint setting for this pipeline
only.
If not specified (NULL/missing), respects \code{getOption("gdalcli.checkpoint", FALSE)}.
To enable checkpointing by default, use \code{gdalcli_options(checkpoint = TRUE)}.
When enabled, saves intermediate results to enable resumption from failures.
Default: \code{FALSE} (disabled unless configured via \code{gdalcli_options()}).}

\item{checkpoint_dir}{Character. Directory for checkpoint files. If not specified,
uses \code{getOption("gdalcli.checkpoint_dir", NULL)} or current working directory
if checkpointing is enabled. Set via \code{gdalcli_options(checkpoint_dir = "/path")}.}

\item{resume}{Logical. Resume pipeline execution from the most recent checkpoint.
If checkpoint is enabled and a checkpoint exists at the checkpoint directory,
set \code{resume = TRUE} to continue from where execution stopped. Default:
\code{FALSE}.}

\item{...}{Additional arguments passed to individual job execution.}

\item{verbose}{Logical. If \code{TRUE}, prints progress information. Default \code{FALSE}.}
}
\value{
Invisibly returns \code{TRUE} on successful completion, or (in native mode with
streaming output) returns the captured output. When checkpointing is enabled
and
completes successfully, the checkpoint directory is cleaned up.
}
\description{
Executes a sequence of GDAL jobs in order, with output from one job
potentially becoming input to the next through virtual file systems.
}
\examples{
\dontrun{
# Sequential execution (default, each job runs separately)
pipeline <- gdal_raster_reproject(
  input = "in.tif",
  dst_crs = "EPSG:32632"
) |>
  gdal_raster_scale(
    src_min = 0, src_max = 100,
    dst_min = 0, dst_max = 255
  ) |>
  gdal_raster_convert(output = "out.tif")
gdal_job_run(pipeline)

# Native pipeline execution (single GDAL pipeline command)
gdal_job_run(pipeline, execution_mode = "native")

# Enable checkpointing globally (opt-in)
gdalcli_options(checkpoint = TRUE)
gdal_job_run(pipeline)  # Now automatically checkpoints!

# Enable checkpoints with custom directory
gdalcli_options(
  checkpoint = TRUE,
  checkpoint_dir = "~/my_checkpoints"
)

# Resume from checkpoint if interrupted
gdal_job_run(pipeline, resume = TRUE)
}

}
\seealso{
\code{\link[=render_gdal_pipeline]{render_gdal_pipeline()}}, \code{\link[=gdal_job_run.gdal_pipeline]{gdal_job_run.gdal_pipeline()}}
}
