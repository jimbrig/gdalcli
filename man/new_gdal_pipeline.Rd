% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-gdal_pipeline.R
\name{new_gdal_pipeline}
\alias{new_gdal_pipeline}
\title{Define and Create a GDAL Pipeline Specification}
\usage{
new_gdal_pipeline(jobs, name = NULL, description = NULL)
}
\arguments{
\item{jobs}{A list of \code{gdal_job} objects.}

\item{name}{Optional character string naming the pipeline.}

\item{description}{Optional character string describing the pipeline.}
}
\value{
An S3 object of class \code{gdal_pipeline}.
}
\description{
A \code{gdal_pipeline} object is an S3 list with the following slots:
\itemize{
\item \strong{jobs} (\code{list}): A list of \code{gdal_job} objects to be executed in sequence.
\item \strong{name} (\code{character(1)}): Optional name for the pipeline.
\item \strong{description} (\code{character(1)}): Optional description of the pipeline.
}
}
\section{Constructor}{


The \code{new_gdal_pipeline()} function creates a new \code{gdal_pipeline} object.
End users typically interact with pipelines through the \verb{|>} operator.
}

\examples{
\dontrun{
# Create individual jobs
job1 <- gdal_raster_info("input.tif")
job2 <- gdal_raster_convert(input = "/vsistdout/", output = "output.jpg")

# Create pipeline
pipeline <- new_gdal_pipeline(list(job1, job2))

# Execute pipeline
gdal_job_run(pipeline)
}

}
\seealso{
\link{gdal_job}, \code{\link[=gdal_job_run]{gdal_job_run()}}, \code{\link[=render_gdal_pipeline]{render_gdal_pipeline()}},
\code{\link[=render_shell_script]{render_shell_script()}}
}
