% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-options.R
\name{gdalcli_options}
\alias{gdalcli_options}
\title{Configure GDALCLI Global Options}
\usage{
gdalcli_options(
  checkpoint = NULL,
  checkpoint_dir = NULL,
  backend = NULL,
  verbose = NULL,
  audit_logging = NULL,
  stream_out_format = NULL,
  ...
)
}
\arguments{
\item{checkpoint}{Logical. Enable/disable automatic checkpointing for pipelines.
When TRUE, pipelines automatically save intermediate results to enable
resumption
from failures. Default: FALSE (disabled).}

\item{checkpoint_dir}{Character. Directory for saving checkpoint files.
Defaults to current working directory if checkpointing enabled and this is
NULL.
Example: \code{"~/gdalcli_checkpoints"}. Ignored if checkpoint = FALSE.}

\item{backend}{Character. Default execution backend: "processx" (subprocess),
"gdalraster" (C++ bindings), "reticulate" (Python), or "auto" (auto-select).
Default: "auto".}

\item{verbose}{Logical. Print execution details by default. Default: FALSE.}

\item{audit_logging}{Logical. Log all job executions. Default: FALSE.}

\item{stream_out_format}{Character. Default output streaming format:
NULL (no streaming), "text", "raw", "json", or "stdout". Default: NULL.}

\item{...}{Additional arguments (not allowed; raises error if provided).}
}
\value{
Invisibly returns a list of current gdalcli options (before modification).
}
\description{
Provides a convenient interface for setting gdalcli configuration options.
Options control behavior of pipeline execution, checkpointing, and output.
}
\section{Checkpoint Configuration}{
Checkpoints are \strong{disabled by default} for backward compatibility. Enable
them
explicitly when you need fault tolerance for long-running pipelines:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Enable checkpoints globally, use current working directory
gdalcli_options(checkpoint = TRUE)

# Enable checkpoints with custom directory
gdalcli_options(
  checkpoint = TRUE,
  checkpoint_dir = "~/gdalcli_checkpoints"
)

# Disable checkpoints
gdalcli_options(checkpoint = FALSE)
}\if{html}{\out{</div>}}
}

\section{Execution Behavior}{
When checkpoints are enabled globally:
\itemize{
\item Pipelines automatically save intermediate outputs after each step
\item If interrupted, call the same pipeline with \code{resume = TRUE} to continue
\item Checkpoint directory defaults to current working directory
\item On successful completion, checkpoints are cleaned up automatically
}
}

\section{Other Options}{
\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Set default execution backend
gdalcli_options(backend = "gdalraster")

# Enable verbose output and audit logging
gdalcli_options(verbose = TRUE, audit_logging = TRUE)

# Set default output streaming
gdalcli_options(stream_out_format = "json")
}\if{html}{\out{</div>}}
}

\examples{
\dontrun{
# Check current options
gdalcli_options()

# Enable checkpoints in custom directory
gdalcli_options(
  checkpoint = TRUE,
  checkpoint_dir = "~/checkpoints/gdalcli"
)

# Now pipelines will automatically checkpoint
pipeline <- gdal_raster_reproject(
  input = "large.tif",
  dst_crs = "EPSG:4326"
) |>
  gdal_raster_convert(output = "output.tif")
gdal_job_run(pipeline)  # Automatically checkpoints!

# If interrupted, resume with:
gdal_job_run(pipeline, resume = TRUE)
}

}
