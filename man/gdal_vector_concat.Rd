% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdal_vector_concat.R
\name{gdal_vector_concat}
\alias{gdal_vector_concat}
\title{concat: Concatenate vector datasets}
\usage{
gdal_vector_concat(
  input,
  input_format = NULL,
  input_layer = NULL,
  source_layer_field_name = NULL,
  source_layer_field_content = NULL,
  output,
  output_format = NULL,
  output_layer = NULL,
  open_option = NULL,
  creation_option = NULL,
  layer_creation_option = NULL,
  overwrite = FALSE,
  update = FALSE,
  overwrite_layer = FALSE,
  append = FALSE,
  mode = NULL,
  field_strategy = NULL,
  src_crs = NULL,
  dst_crs = NULL
)
}
\arguments{
\item{input}{Input vector datasets (required). \code{1} to \code{2147483647} value(s). Can also be a \link{gdal_job} object to extend a pipeline}

\item{input_format}{Input formats (Character vector) (Advanced)}

\item{input_layer}{Input layer name(s) (Character vector)}

\item{source_layer_field_name}{Name of the new field to add to contain identificoncation of the source layer, with value determined from 'source-layer-field-content'}

\item{source_layer_field_content}{A string, possibly using \{AUTO_NAME\}, \{DS_NAME\}, \{DS_BASENAME\}, \{DS_INDEX\}, \{LAYER_NAME\}, \{LAYER_INDEX\}}

\item{output}{Output vector dataset (Dataset path) (required)}

\item{output_format}{Output format ("GDALG" allowed)}

\item{output_layer}{Name of the output vector layer (single mode), or template to name the output vector layers (stack mode)}

\item{open_option}{Open options (Character vector). Format: \verb{<KEY>=<VALUE>} (Advanced)}

\item{creation_option}{Creation option (Character vector). Format: \verb{<KEY>=<VALUE>}}

\item{layer_creation_option}{Layer creation option (Character vector). Format: \verb{<KEY>=<VALUE>}}

\item{overwrite}{Whether overwriting existing output is allowed (Logical) (Default: \code{false})}

\item{update}{Whether to open existing dataset in update mode (Logical) (Default: \code{false})}

\item{overwrite_layer}{Whether overwriting existing layer is allowed (Logical) (Default: \code{false})}

\item{append}{Whether appending to existing layer is allowed (Logical) (Default: \code{false})}

\item{mode}{Determine the strategy to create output layers from source layers . Choices: "merge-per-layer-name", "stack", "single" (Default: \code{merge-per-layer-name})}

\item{field_strategy}{How to determine target fields from source fields. Choices: "union", "intersection" (Default: \code{union})}

\item{src_crs}{Source CRS}

\item{dst_crs}{Destination CRS}
}
\value{
A \link{gdal_job} object.
}
\description{
\verb{gdal vector concat} concatenates several source datasets. It has 3 main modes: - --mode = \code{merge-per-layer-name} (the default). The output dataset generated by the command will contain as many layers as there are different layer names in the source datasets. For example if there are 2 datasets, one with layers \code{a} and \code{b}, and the other one with layers \code{b} and \code{c}, 3 output layers will be created: \code{a}, \code{b} (merging the 2 source layers) and \code{c}. - --mode = \code{stack}. The output dataset generated by the command will contain as many layers as there are layers in the source datasets. For example if there are 2 datasets \code{ds1} (with layers \code{a} and \code{b}) and \code{ds2} (with layers \code{b} and \code{c}), 4 output layers will be created: \code{ds1_a}, \code{ds1_b}, \code{ds2_b} and \code{ds2_c}. - --mode = \code{single}. The output dataset generated by the command will contain one single layer, merging all layers in the source datasets. When an output layer merges several source layer, by default the resulting schema will contain the union of all source fields. It is possible to select only the intersection with the --field-strategy set to \code{intersection}. Regarding the resulting CRS, by default the CRS of the source layer will be used as the target CRS, and features of other source layers that do no match this CRS will be reprojected to it. --dst-crs can be used to select a given destination CRS. This command can also be used as the first step of gdal_vector_pipeline.

See \url{https://gdal.org/en/release-3.11/programs/gdal_vector_concat.html} for detailed GDAL documentation.
}
\examples{
job <- gdal_vector_concat(input = "*.shp", output = "out.gpkg", mode = "stack")
\dontrun{
  result <- gdal_job_run(job)
}
}
\seealso{
Other gdal_vector_utilities: 
\code{\link{gdal_vector}()},
\code{\link{gdal_vector_clip}()},
\code{\link{gdal_vector_convert}()},
\code{\link{gdal_vector_edit}()},
\code{\link{gdal_vector_filter}()},
\code{\link{gdal_vector_geom_buffer}()},
\code{\link{gdal_vector_geom_explode_collections}()},
\code{\link{gdal_vector_geom_make_valid}()},
\code{\link{gdal_vector_geom_segmentize}()},
\code{\link{gdal_vector_geom_set_type}()},
\code{\link{gdal_vector_geom_simplify}()},
\code{\link{gdal_vector_geom_swap_xy}()},
\code{\link{gdal_vector_grid_average}()},
\code{\link{gdal_vector_grid_average_distance}()},
\code{\link{gdal_vector_grid_average_distance_points}()},
\code{\link{gdal_vector_grid_count}()},
\code{\link{gdal_vector_grid_invdist}()},
\code{\link{gdal_vector_grid_invdistnn}()},
\code{\link{gdal_vector_grid_linear}()},
\code{\link{gdal_vector_grid_maximum}()},
\code{\link{gdal_vector_grid_minimum}()},
\code{\link{gdal_vector_grid_nearest}()},
\code{\link{gdal_vector_grid_range}()},
\code{\link{gdal_vector_info}()},
\code{\link{gdal_vector_pipeline}()},
\code{\link{gdal_vector_rasterize}()},
\code{\link{gdal_vector_reproject}()},
\code{\link{gdal_vector_select}()},
\code{\link{gdal_vector_sql}()}
}
\concept{gdal_vector_utilities}
